import SwiftUI
import PagerunnerCore

// Stub — replaced in Task 8
struct ProfileDetailView: View {
    let appState: AppState
    let profileName: String
    let controller: StatusItemController
    var body: some View { Text("Profile: \(profileName)") }
}
